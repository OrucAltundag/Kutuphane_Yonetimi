@using Kutuphane_Yonetimi.Models.Entity
@model List<TBL_PERSONEL>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2>Personeller</h2>
    <div class="d-flex align-items-center">
        <input type="text" id="searchBox" class="form-control me-2" placeholder="Personel ara..." />
    </div>
    <a href="/Personel/PersonelEkle/" class="btn btn-primary">
        <i class="bi bi-plus-circle"></i> Yeni Personel Ekle
    </a>
</div>

<table class="table table-striped table-hover scroll-table">
    <thead class="table-dark">
        <tr>
            <th>ID</th>
            <th>Personel Adı ve Soyadı</th>
            <th>Sil</th>
            <th>Güncelle</th>
        </tr>
    </thead>
    <tbody id="personelTable">
        @foreach (var per in Model)
        {
            <tr>
                <td>@per.ID</td>
                <td>@per.PERSONEL</td>
                <td>
                    <a href="/Personel/PersonelSil/@per.ID" class="btn btn-danger btn-sm">
                        <i class="bi bi-trash"></i> Sil
                    </a>
                </td>
                <td>
                    <a href="/Personel/PersonelGetir/@per.ID" class="btn btn-success btn-sm">
                        <i class="bi bi-pencil"></i> Güncelle
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">

<script>
    document.getElementById('searchBox').addEventListener('keyup', function () {
        let filter = this.value.toLowerCase();
        document.querySelectorAll('#personelTable tr').forEach(row => {
            let PersonelAdi = row.cells[1].textContent.toLowerCase();
            row.style.display = PersonelAdi.includes(filter) ? '' : 'none';
        });
    });
</script>


<style>
    .scroll-table thead,
    .scroll-table tbody tr {
        display: table;
        width: 100%;
        table-layout: fixed;
    }

    .scroll-table tbody {
        display: block;
        max-height: 400px; /* Yaklaşık 10 satır için */
        overflow-y: auto;
    }
</style>

